-- Seed data for development

-- Note: Profiles are auto-created by the trigger when users sign up.
-- For local development, create test users via Supabase Auth UI or CLI.

-- Sample extensions (will need valid publisher_id from auth.users)
-- These are inserted via the dashboard or CLI in practice.

-- Categories reference (stored as extension.categories TEXT[]):
-- ai, automation, communication, data, developer-tools, education,
-- finance, media, productivity, security, social, utilities, weather, web
